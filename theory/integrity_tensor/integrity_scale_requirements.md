# Integrity Scale Requirements

## Purpose

This document establishes the mandatory requirements that any future TIG Integrity Tensor candidate must satisfy.

The requirements are derived from the audit failure of the Organizational Integrity Tensor.

No new tensor is proposed in this document.

The objective is to define the mathematical constraints that any future candidate must satisfy.

---

# Origin

The Organizational Integrity Tensor audit concluded:

```text
FAIL
```

Reason:

```text
Missing integrity scale r_c.
```

The candidate was mathematically valid but unable to reproduce the TIG horizon structure.

---

# Fundamental Observation

The defining structural quantity of TIG is:

```text
r_c
```

which appears through:

```text
β = r_c/(2M)
```

and enters the horizon equation:

```text
x³ - x² + β³ = 0
```

Therefore:

```text
r_c
```

is not an optional parameter.

It is a structural quantity.

---

# Requirement R1

## Integrity Scale Presence

Any future Integrity Tensor must:

```text
contain,
generate,
or recover
r_c
```

within its mathematical structure.

Status:

```text
MANDATORY
```

---

# Requirement R2

## Beta Reconstruction

The tensor must permit construction of:

```text
β = r_c/(2M)
```

Status:

```text
MANDATORY
```

---

# Requirement R3

## Horizon Equation Recovery

The tensor must admit derivation of:

```text
x³ - x² + β³ = 0
```

either directly or through the resulting field equations.

Status:

```text
MANDATORY
```

---

# Requirement R4

## General Relativity Limit

For:

```text
r_c → 0
```

the resulting theory must recover the General Relativity limit.

Status:

```text
MANDATORY
```

---

# Requirement R5

## Finite Curvature Compatibility

The tensor must support bounded-curvature sectors.

The existence of:

```text
r_c > 0
```

must prevent singular behavior at:

```text
r = 0
```

Status:

```text
MANDATORY
```

---

# Requirement R6

## Structural Origin

The integrity scale must arise from the tensor construction itself.

The scale may not be inserted artificially after tensor construction.

Forbidden:

```text
Tensor
↓
finished
↓
add r_c later
```

Required:

```text
r_c
↓
tensor construction
```

Status:

```text
MANDATORY
```

---

# Requirement R7

## Covariance

The tensor must remain covariant.

Status:

```text
MANDATORY
```

---

# Requirement R8

## Rank-Two Structure

The tensor must be a covariant rank-two tensor.

Status:

```text
MANDATORY
```

---

# Requirement R9

## Conservation

The tensor must satisfy:

```text
∇μ Iμν = 0
```

either identically or through the field equations.

Status:

```text
MANDATORY
```

---

# Requirement R10

## TIG-Native Origin

The tensor should emerge from a quantity already present in the TIG framework.

Preferred sources include:

```text
r_c
β
horizon structure
integrity scale
bounded-curvature structure
```

Status:

```text
STRONGLY PREFERRED
```

---

# Audit Constraint

Any future candidate failing Requirement R1 is automatically rejected.

Reason:

```text
A tensor without r_c cannot reproduce the TIG horizon structure.
```

---

# Current Status

Organizational Tensor:

```text
FAILED
```

Reason:

```text
Missing r_c.
```

Active Research Object:

```text
r_c
```

Open Question:

```text
Can a covariant rank-two tensor be constructed
whose fundamental quantity is the integrity scale r_c?
```
