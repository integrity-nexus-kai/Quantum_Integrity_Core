\documentclass[11pt,a4paper]{article}

\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{hyperref}

\graphicspath{{./}}

\title{Topological Integrity Gravity (TIG):\
A Quadratic $f(R)$ Model with a Critical Horizon Transition}

\author{
Kai Stefan Dietrich \
Independent Research
}

\date{\today}

\begin{document}

\maketitle

% Abstract (EN + DE)
\input{abstract}

% --------------------------------------------------
\section{Introduction}

General Relativity was first formulated by Einstein~\cite{einstein1915} and remains the foundation of modern gravitational theory~\cite{wald1984}.

Among its extensions, $f(R)$ gravity has been extensively studied~\cite{defelice2010,sotiriou2010}, with the quadratic model originally proposed by Starobinsky~\cite{starobinsky1980}.

In this work, we investigate Topological Integrity Gravity (TIG) as a constrained quadratic $f(R)$ framework and explore its implications for compact objects and observable signatures.

% --------------------------------------------------
\section{Formalism}

We consider the action:
\begin{equation}
S = \int d^4x \sqrt{-g}
\left[
\frac{1}{16\pi}(R - 2\Lambda) + \alpha R^2
\right]

* S_{\mathrm{matter}}.
  \end{equation}

Defining
\begin{equation}
f(R) = R - 2\Lambda + \mu R^2, \quad \mu = 16\pi\alpha,
\end{equation}
the model corresponds to a quadratic $f(R)$ theory.

% --------------------------------------------------
\section{Field Equations}

The modified field equations are:
\begin{equation}
f'(R) R_{\mu\nu}

* \frac{1}{2} f(R) g_{\mu\nu}

- (g_{\mu\nu}\Box - \nabla_\mu \nabla_\nu) f'(R)
  = 8\pi T_{\mu\nu}.
  \end{equation}

with
\begin{equation}
f'(R) = 1 + 2\mu R.
\end{equation}

The trace equation reads:
\begin{equation}
3\Box f'(R) + f'(R)R - 2f(R) = 8\pi T.
\end{equation}

% --------------------------------------------------
\section{Static Configurations}

We consider static, spherically symmetric geometries:
\begin{equation}
ds^2 =
-F(r)dt^2
+
\frac{dr^2}{F(r)}
+
r^2 d\Omega^2.
\end{equation}

\subsection{Effective Interior Profile}

We consider the mass profile:
\begin{equation}
m(r) = \frac{M r^3}{r^3 + r_c^3}.
\end{equation}

The metric function is:
\begin{equation}
F(r) = 1 - \frac{2m(r)}{r}.
\end{equation}

% --------------------------------------------------
\subsection{Horizon Structure}

Horizons satisfy:
\begin{equation}
F(r_H) = 0.
\end{equation}

This leads to:
\begin{equation}
r_H^3 - 2M r_H^2 + r_c^3 = 0.
\end{equation}

Defining dimensionless variables:
\begin{equation}
x = \frac{r_H}{2M}, \quad \beta = \frac{r_c}{2M},
\end{equation}
we obtain:
\begin{equation}
x^3 - x^2 + \beta^3 = 0.
\end{equation}

% --------------------------------------------------
\subsection{Critical Transition}

The cubic equation exhibits a critical value:
\begin{equation}
\beta_c = \left(\frac{4}{27}\right)^{1/3},
\end{equation}

For:
\begin{itemize}
\item $\beta \leq \beta_c$: horizons exist,
\item $\beta > \beta_c$: no real positive solution exists.
\end{itemize}

This transition separates black hole configurations from horizonless compact objects.

% --------------------------------------------------
\subsection{Numerical Illustration}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{tig_horizon_radius_prediction.png}
\caption{Normalized horizon radius as a function of $\beta$.}
\end{figure}

% --------------------------------------------------
\section{Photon Sphere and Shadow Radius}

The photon sphere is defined by:
\begin{equation}
rF'(r) - 2F(r) = 0.
\end{equation}

This yields:
\begin{equation}
2x^6 - 3x^5 + 4\beta^3x^3 + 2\beta^6 = 0.
\end{equation}

The shadow radius is given by:
\begin{equation}
R_{\mathrm{sh}} = \frac{r_{\mathrm{ph}}}{\sqrt{F(r_{\mathrm{ph}})}}.
\end{equation}

% --------------------------------------------------
\section{Observational Implications}

The modified horizon and photon-sphere structure may be probed via:

\begin{itemize}
\item black hole imaging,
\item gravitational lensing,
\item gravitational-wave observations.
\end{itemize}

% --------------------------------------------------
\section{Conclusion}

We have studied TIG as a constrained quadratic $f(R)$ gravity model.

A key feature is the existence of a critical parameter:
\begin{equation}
\beta_c = \left(\frac{4}{27}\right)^{1/3},
\end{equation}

which separates black hole solutions from horizonless compact objects.

These results suggest potential observational signatures that could test deviations from General Relativity.

% --------------------------------------------------
\bibliographystyle{unsrt}
\bibliography{references}

\end{document}
